# Get this from app.ourskylight.com → Chrome DevTools → Network → any request → Authorization: Bearer <token>
SKYLIGHT_TOKEN=paste_your_token_here

# Optional overrides
# SKYLIGHT_FRAME_ID=   (auto-detected)
# SKYLIGHT_TIMEZONE=America/New_York  (auto-detected)
PORT=3000
